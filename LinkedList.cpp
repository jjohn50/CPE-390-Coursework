#include <iostream>
using namespact std; 
//ArrayList (Java)
//Vector C++

class GrowArray(){
private:
	int* data; 
	int len; 
public: 
};

int main(){
	int x[1000]={1,2,3}; 
	GrowArray a; //Create an empty lis
	a.add(3); 
	a.add(1);
	a.add(4);
	for(int i = 0; i < 10; i++){
		a.add(i);
	}
}